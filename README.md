# Freezeng 知识库

基于 [Quartz](https://quartz.jzhao.xyz/) 的个人知识库，内容分为计算数学、计算机科学和推荐系统理论。

## 本地预览

```bash
npm ci
npx quartz plugin install
npx quartz build --serve
```

内容位于 `content/`，推送到 `main` 后由 GitHub Actions 发布到 GitHub Pages。

> OneRec 区域是临时评审稿，公开发布前必须再次执行脱敏检查；完成内部评审后应删除或迁移到私有仓库。
